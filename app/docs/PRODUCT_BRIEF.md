# Product Brief

## 目的

プレイ経験を本人の学びへ変換する。単なる戦績表示やAIによる一方的な指導ではなく、映像と状況を結びつけ、本人が「何が起きていたか」に気づける振り返り体験を作る。

## 実装状況

現在の実アプリでは、録画監視、自動試合分割、処理状態一覧、実動画再生、試合終了後の勝敗発表判定、自分のデス検出とAI説明、自分のブキ名照合、敵味方の生存人数・人数差グラフ、ゲームカウントOCR、プレイヤーパネルの点線で裏付けた味方位置のマップ観測まで動作する。デス直前の相手色・形状・動きは説明生成の内部根拠に限り、動画上へ敵候補枠は表示しない。自分位置・動線、人物の確定検出、敵のブキ名、実際のカメラ姿勢を使った座標対応は今後の解析実装対象である。未取得のデータをサンプル値で補わず、画面に解析範囲を明示する。

## 主要画面

動画、ステージマップ、デス分析を並列表示する。すべての表示は動画時刻へ同期する。

### 動画

- ローカル動画を再生する
- デスをシークバーへ表示する
- 敵候補オーバーレイは表示しない

### ステージマップ

- 自分の試合全体の動線を薄く残し、現在時刻付近を強調する
- 視認できた敵・味方の位置を表示する
- 最後に視認した位置と移動方向から、短時間の予想動線を表示する
- 実際に確認できた情報と予想を明確に区別する

### 試合フロー

- シークバーとグラフの左右端・時間軸を一致させる
- 自チームと相手のカウント推移を表示する
- 試合終了後の左上に表示される `WIN!` / `LOSE...` を複数フレームで確認し、一覧の勝敗へ優先使用する
- 生存人数から人数有利・五分・人数不利を連続した帯で表示する
- 自分がデスした瞬間を赤い縦線と菱形で表示する
- シークバー直下・グラフ直上へ、現在時刻・両チームのカウント・人数差を表示する
- グラフクリックで動画を同時刻へシークする
- 独立した再生・±5秒・速度調整列と、グラフタイトルは置かない

### デス分析

- デスだけを一覧表示する
- 選択時は8秒前へジャンプし、状況と直接原因を表示する
- 画面で確認できた事実と推測を分離し、読めないブキ名・攻撃方法・敵位置を断定しない

## データ原則

- 動画ファイルと解析JSONを分離する
- 解析JSONは最低限 `events` と `gameFlow` を持つ
- 座標・時刻・推定根拠を検証してから表示する
- リザルトのデス数はローカルOCRで読み、外部AIや従量課金APIへ送らない
- AIデス分析が有効な場合だけ、抽出したデス前後画像をChatGPTプランで認証したOpenAI実行環境へ送る

## 参照実装

`../../mock` に静的HTML/CSS/JavaScriptのモックを保存している。アプリ本体ではモックを直接拡張せず、必要な仕様を移植する。
