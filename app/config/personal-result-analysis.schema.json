{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "required": ["matches"],
  "properties": {
    "matches": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "stage", "rule", "weapon", "kills", "deaths", "confidence", "evidence"],
        "properties": {
          "id": { "type": "string" },
          "stage": { "type": "string" },
          "rule": { "type": "string" },
          "weapon": { "type": "string" },
          "kills": { "type": "integer", "minimum": 0, "maximum": 99 },
          "deaths": { "type": "integer", "minimum": 0, "maximum": 99 },
          "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
          "evidence": { "type": "string" }
        }
      }
    }
  }
}
