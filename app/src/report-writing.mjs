export const REPORT_WRITING_VERSION = 1;

export const REPORT_WRITING_GUIDANCE = `完成文はプレイヤー向けの試合振り返りです。
サマリは2〜3文で、負けにつながった具体的な行動パターンと、それが攻め・守り・目的物への関与をどう損なったかを最初に伝えてください。試合全体の敗因を証拠以上に断定せず、確認できた範囲で述べてください。勝った試合を負けと扱わないでください。
ステージ・ルール・使用ブキ・勝敗・最終スコアなど、試合レビュー画面に表示済みの情報を紹介・列挙しないでください。ただし、判断の説明に必要なその時点の人数差・カウント・攻撃の特徴は具体的に書いて構いません。
分析した手順、ツール、データ照合、検出の誤り、時刻補正の有無、offset、death.timeなどの内部仕様は、サマリにも各場面の本文にも書かないでください。データの誤りは調査時に考慮し、完成文にはプレイの説明を残してください。
ユーザー向けの文字列でdeath-1などの内部IDを使わず、「デス1回目」「デス1回目と3回目」のように表現してください。JSONのid/deathIds/deathIdおよび数値offsetは機械処理用なので指定どおり保持してください。
抽象的な批判だけで終えず、状況→繰り返した行動→不利になった理由をつなげてください。成果のあった行動まで一括で否定せず、改善対象を絞ってください。分析の信頼性に関わる不確かさは「攻撃元は特定できない」などプレイの理解に必要な一言に絞ってください。`;

export function playerFacingText(value, maxLength) {
  if (typeof value !== 'string') return '';
  return value.replace(/\bdeath-([1-9]\d*)\b/g, 'デス$1回目').trim().slice(0, maxLength);
}
