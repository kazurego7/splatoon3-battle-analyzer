# Data

大容量動画とアプリの自動生成物を置く領域です。この案内ファイル以外はプロジェクトのGit管理に含まれません。

- `raw/`: 加工前の録画。通常、手で追加するのはここだけです。
- `matches/`: 試合単位へ自動分割した動画
- `app/`: アプリの状態、試合解析JSON、サムネイル
- `work/`: 再生成可能な検出キャッシュ、一時クリップ、QA画像

旧手動解析の画像は `archive/legacy-analysis/output/` へ退避しています。
