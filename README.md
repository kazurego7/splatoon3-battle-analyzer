# splatoon3-battle-analyzer

スプラトゥーン3のバトル分析アプリです。

キャプチャーボードで録画した長時間動画を、試合ごとに自動で分割・分析し、ブラウザで振り返るためのローカルアプリです。録画や解析結果はPC内に保存され、通常の利用では外部サービスへ送信されません。

## できること

- 長時間録画から試合区間を見つけ、試合単位の動画に分割
- ルール名・ステージ名・自分のブキをリザルト画面から判定
- 勝敗発表、デス、敵味方の生存人数、ゲームカウント、ペナルティを自動判定
- 各デスの行動シーケンスをAIで整理し、同じ試合で繰り返す失敗パターンを抽出（任意）
- 動画、グラフ、マップ画面を同じ時刻に連動
- ステージ俯瞰画像を使って試合中の位置情報を振り返り

## 必要なもの

- Windows 10 / 11
- Node.js 22以上
- MP4などのゲーム録画
- FFmpeg（環境変数 `FFMPEG_PATH` で `ffmpeg.exe` を指定）

AIによるデス場面の説明は任意機能です。使う場合だけCodexへログインしてください。ローカル解析と通常の画面表示には不要です。

## はじめかた

PowerShellでプロジェクトを開き、次を実行します。

```powershell
cd app
npm install
npm start
```

ブラウザで <http://127.0.0.1:4310> を開きます。起動中に `data/raw/` へ録画を置くと、通常は5秒以内に処理が始まります。

処理状況は「待機中 → 確認中 → 分割中 → 分析中 → 分析済み」の順に表示されます。完了した録画を選び、試合一覧から見たい試合を開いてください。

## データの保存場所

| 内容 | 場所 |
|---|---|
| 入力する元録画 | `data/raw/` |
| 分割された試合動画 | `data/matches/` |
| 解析結果・サムネイル | `data/app/` |
| 再生成できる一時データ | `data/work/` |
| ステージ俯瞰画像 | `assets/stage-maps/images/` |

`data/`、ローカル実行環境、確認用の一時画像、ダウンロードしたステージ画像、ローカル生成したブキ辞書はGitへ入りません。元録画や第三者素材を誤って公開しないため、プロジェクト直下の `.gitignore` で一括して除外しています。

## 任意: AIによるデス分析

```powershell
cd app
npx codex login
npm start
```

試合の分析画面で「AI分析」を押すと、各デスの8秒前から直後までの6コマをCodexで分析し、2件以上で繰り返す失敗パターンを抽出します。試合画面ではレポートダイジェストの下にデスを縦一覧で表示し、各項目を選ぶと映像の8秒前へ移動します。「俯瞰レポート」からは長い分析を別ウィンドウで開けます。レポートではパターンごとに独立した動画を置き、根拠場面を順にループするクリップモードと、試合全体を自由に見る通常再生を動画ごとに切り替えられます。動画ファイル自体は切り抜かず、元の試合動画上に複数の範囲として表示します。

AIを使わない限り映像由来画像は外部へ送信されません。APIキーへの自動切り替えは行いません。

録画の初回解析時にも自動実行したい場合だけ、起動前に `$env:CODEX_DEATH_ANALYSIS = "true"` を設定してください。画面のボタンはこの環境変数がなくても利用できます。

## ステージ画像を更新する

リポジトリには `$sync-splatoon-stage-maps` スキルを同梱しています。Codexへ「ステージ画像を最新版へ更新して」と依頼すると、画像の取得、版番号の比較、WebP検証、欠損・重複チェック、出典一覧の更新をまとめて実行します。画像は第三者素材のためGit管理外で、利用者自身が取得元の利用条件を確認したうえでローカル利用します。

画像を取得済みの場合は、スキル内のスクリプトで手元の素材だけを検査できます。

```powershell
python .agents/skills/sync-splatoon-stage-maps/scripts/sync_stage_maps.py check
```

## よくある問題

- **画面が開かない**: Node.jsが22以上か確認し、`app/` で `npm install` を実行してください。
- **動画処理でFFmpegエラーになる**: `FFMPEG_PATH` に利用可能な `ffmpeg.exe` のフルパスを設定してください。
- **録画を置いても始まらない**: `data/raw/` 直下に動画があることと、アプリを起動したままにしていることを確認してください。
- **解析結果を作り直したい**: 画面の再分析ボタンを使うか、`app/` で `npm run analyze -- "録画ファイル名"` を実行してください。

## 開発・検証

```powershell
cd app
npm run check
npm test
npm run check:public
```

詳しい解析データ形式は [`app/docs/ANALYSIS_SCHEMA.md`](app/docs/ANALYSIS_SCHEMA.md)、現在の実装範囲は [`app/README.md`](app/README.md) を参照してください。

## プロジェクト構成

```text
.
├─ app/             Webアプリ、解析処理、自動テスト
├─ assets/          アプリが使う共有素材
├─ data/            録画と生成データ（Git管理外）
├─ mock/            UIの参照モック
├─ tools/           FFmpegなどのローカル実行環境（Git管理外）
├─ archive/         旧検証物（README以外はGit管理外）
└─ .agents/skills/  プロジェクト専用Codexスキル
```

## ライセンスと第三者素材

このプロジェクトが独自に作成したコードと文書は [MIT License](LICENSE) で公開します。Splatoonおよび関連するゲーム内容、録画、スクリーンショット、ダウンロード画像、抽出データ、ローカル生成辞書はMIT Licenseの対象外で、リポジトリにも収録しません。詳細は [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md) を参照してください。

このリポジトリは任天堂株式会社の公式プロジェクトではなく、任天堂株式会社による承認・提携を示すものではありません。
